/**
 * Hedera client construction for the Oracle and Agent operator accounts —
 * architecture/README.md §16, §18.
 *
 * TODO: Client.forName(env.hederaNetwork).setOperator(accountId, privateKey)
 * for both the Oracle and Agent identities once their env vars are set.
 */
export {};
